services:
  rates-bot:
    build: .
    container_name: rates-bot
    restart: unless-stopped
    env_file: .env
    environment:
      DB_PATH: /data/rates.db
    volumes:
      - rates-data:/data

volumes:
  rates-data:
